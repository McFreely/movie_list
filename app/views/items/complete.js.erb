$('.watched_item').bind('ajax:success', function() {
	$(this).closest('div.item_details').fadeOut();
});

$('<%= escape_javascript(render(:partial => 'items/list_done'))%>')
  .replaceAll('#list_done')

$('#list_done li:first')
  .hide()
  .delay(400)
  .fadeIn()

